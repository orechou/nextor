<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Enable App Sandbox (Required for Mac App Store) -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Allow accessing files selected by user through file dialogs -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Network client access (for Mermaid charts and external resources) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Disable network server (not needed) -->
    <key>com.apple.security.network.server</key>
    <false/>

    <!-- Print permission (for PDF export) -->
    <key>com.apple.security.print</key>
    <true/>
</dict>
</plist>
